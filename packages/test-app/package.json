{
  "name": "@urturn/test-app",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run start:runner\" \"npm run start:frontend\"",
    "start:runner": "runner --frontend-port 3000 --debug --dev",
    "start:frontend": "cd frontend && cross-env BROWSER=none PORT=3000 npm start",
    "lint": "eslint . --ignore-path .gitignore --ext .js && cd frontend && npm run lint",
    "lint:fix": "eslint . --ignore-path .gitignore --ext .js --fix && cd frontend && npm run lint:fix",
    "postinstall": "cd frontend && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@urturn/runner": "*",
    "cross-env": "^7.0.3"
  },
  "devDependencies": {
    "concurrently": "^7.1.0",
    "eslint": "^8.21.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.26.0"
  }  
}
