{
  "name": "@urturn/test-app",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "runner --frontend-port 3000",
    "dev": "concurrently \"npm run dev:runner\" \"npm run dev:frontend\"",
    "dev:runner": "runner --frontend-port 3000 --debug --dev",
    "dev:frontend": "BROWSER=none PORT=3000 npx lerna run start --scope=@urturn/test-app-frontend",
    "lint": "eslint . --ignore-path .gitignore --ext .js",
    "lint:fix": "eslint . --ignore-path .gitignore --ext .js --fix"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@urturn/runner": "*",
    "@urturn/runner-frontend": "*"
  },
  "devDependencies": {
    "concurrently": "^7.1.0",
    "eslint": "^8.21.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.26.0"
  }
}
